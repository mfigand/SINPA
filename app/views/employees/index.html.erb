<h2 class="title is-2"><%= @branch.name%>`s employees </h2>
<br>


<ul>
  <% @employees.each_with_index do |employee, index| %>
    <li> </li>
    <li><strong>Name: </strong><a href="<%= company_branch_employee_path(current_company.id, @branch.id, employee.id) %>"><%= "#{employee.name}" %></a>
       - <a data-method="DELETE" href="<%= company_branch_employee_path(current_company.id, @branch.id, employee.id) %>">Delete</a></li>
    <br>
  <% end %>
</ul>

<a href="<%= new_company_branch_employee_path(current_company.id, @branch.id) %>" class="row btn" >Add Employee</a>
<br>

<a href="<%= company_branch_path(current_company.id, @branch.id) %>">Back to Branch</a>
